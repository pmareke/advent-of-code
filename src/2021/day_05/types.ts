export type Forest = {
  max: number;
  coordinates: Coordinates[];
};

export type Coordinates = {
  x: number;
  y: number;
};

export type Points = {
  x1: number;
  y1: number;
  x2: number;
  y2: number;
};
